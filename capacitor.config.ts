import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'capacitor-esri-leaflet-react-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
